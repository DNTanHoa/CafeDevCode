@using CafeDevCode.Ultils.Model

@model IEnumerable<TreeList<CommentViewModel>>

if(Model != null &&
   Model.Any()) 
{
    <ul>
        @foreach (var item in Model)
        {
            
        }
    </ul>
}
else
{

}

